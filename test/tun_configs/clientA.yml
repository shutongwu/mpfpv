mode: client
teamKey: "tun-test-secret"

client:
  clientID: 1
  # virtualIP left empty for auto-assignment from server.
  serverAddr: "192.168.100.1:9800"
  sendMode: "redundant"
  mtu: 1300
  dedupWindow: 4096
